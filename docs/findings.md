# Findings

Empty until a run writes a manifest. Every claim here names a config file and a run
directory, and leads with the result that came out rather than the one hoped for.

Sections to fill, in this order:

1. Whether Jev traversal matches LLM traversal on section recall, with the interval.
2. Cost and latency at matched recall, the whole pipeline included.
3. The calibration curve and whether thresholds transferred across documents.
4. The escalation rate, and what cost the fallback added back.
5. Where it failed: deep narrow trees, uninformative titles, multi-hop queries.
