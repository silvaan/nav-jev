TYPESAFE_API_KEY=
# Text LLM, chosen per model name in the config: gpt-* uses OPENAI_API_KEY,
# claude-* uses ANTHROPIC_API_KEY. The dense baseline's embeddings also use OpenAI.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
# Optional: reach Jev through Vercel AI Gateway instead of TypeSafe directly.
# AI_GATEWAY_API_KEY=
